---
week: 1
day: 3
date: 2026-06-25
stage: 后端基础与数据库
theme: TypeScript + Node.js 热身
hours: 2
tags: [TypeScript, Node.js, http, middleware, async-await, error-handling]
file: review.md
---

# 今日复盘

## 完成检查清单

- [x] 能解释中间件是什么、`next()` 的作用、不调用 `next()` 的后果。
- [x] `demo/middleware-runner.ts` 可运行，能串联多个中间件。
- [x] `demo/minimal-http-server.ts` 支持完整 CRUD 与统一错误响应。
- [x] 用 curl 验证了正常路径、非法 JSON、资源不存在、404 路径。
- [x] 今日疑问已记录到「卡点记录区」。

## 掌握度自评

请在对应知识点后勾选当前状态：

### 中间件概念与 `next()`
- [ ] 🟢 已掌握：能独立写出串联多个中间件的执行器
- [ ] 🟡 部分理解：能看懂代码，但自己写会卡壳
- [ ] 🔴 不太懂：不清楚 `next()` 到底怎么传递控制权

### 异步错误处理（try / catch / Promise.catch）
- [ ] 🟢 已掌握：能稳定处理同步和异步异常
- [ ] 🟡 部分理解：能跑通示例，但边界情况容易漏
- [ ] 🔴 不太懂：`async` 函数里抛错为什么经常捕获不到

### RESTful 完整 CRUD（GET / POST / PUT / DELETE）
- [ ] 🟢 已掌握：能独立实现资源增删改查
- [ ] 🟡 部分理解：能看懂代码，但状态码和错误处理常写错
- [ ] 🔴 不太懂：对 URL 设计和 Method 语义还不熟

## 复盘问题

1. 今天最大的收获是什么？
2. 中间件模式相比「所有逻辑写在一个 `createServer` 回调」好在哪？
3. 如果 `next()` 忘记调用，会出现什么现象？为什么？
4. 当前代码还有哪些边界情况没有处理？（例如重复 `next()`、body 过大等）

## 明日预习

第 4 天将进入 Express 框架入门前的过渡：用原生 `http` 实现一个更接近 Express 的路由分发器，理解「路由」与「中间件」的关系。

预习要点：
- Express 的 `app.get` / `app.post` 内部如何匹配 URL。
- 路径参数 `:id` 与查询参数 `?category=x` 的区别。
- 如何把今天的中间件执行器扩展为支持「方法 + 路径」匹配的路由表。

## 卡点记录区

> 记录今天没解决、需要后续回顾的问题。

- 问题 1：_________________________
- 问题 2：_________________________

## 今日实际复盘（2026-06-29）

- 完成率：5 / 5（100%）

### 未完成任务

无

### 今日最大收获

（未填写）

### 后续疑问

（未填写）
