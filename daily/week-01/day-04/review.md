---
week: 1
day: 4
date: 2026-06-30
stage: 后端基础与数据库
theme: TypeScript + Node.js 热身
hours: 2
tags: [TypeScript, Node.js, http, router, middleware]
file: review.md
---

# 今日复盘

## 完成检查清单

- [ ] 能解释路由表、`app.get` 内部匹配逻辑、路径参数与查询参数的区别。
- [ ] `demo/router.ts` 可运行，支持 `get/post/put/delete` 注册。
- [ ] 路径参数 `:id` 能正确解析到 `req.params`。
- [ ] 查询参数 `?category=x` 能正确解析到 `req.query`。
- [ ] `demo/minimal-http-server.ts` 用路由表重构了播客 CRUD。
- [ ] 用 curl 验证了正常路径、路径参数、查询参数、非法 JSON、404 路径。
- [ ] 今日疑问已记录到「卡点记录区」。

## 掌握度自评

请在对应知识点后勾选当前状态：

### 路由表设计与 Method + Path 匹配
- [ ] 🟢 已掌握：能独立设计并实现路由分发器
- [ ] 🟡 部分理解：能看懂代码，但自己写会卡壳
- [ ] 🔴 不太懂：不清楚路由匹配和中间件执行器的关系

### 路径参数 `:id` 与查询参数 `?category=x` 解析
- [ ] 🟢 已掌握：能稳定解析并区分使用场景
- [ ] 🟡 部分理解：能跑通示例，但边界情况容易漏
- [ ] 🔴 不太懂：对 `URL` 对象和 `req.url` 的关系不清楚

### 路由与中间件的关系
- [ ] 🟢 已掌握：能解释路由是带条件的中间件
- [ ] 🟡 部分理解：能使用，但讲不清区别
- [ ] 🔴 不太懂：把两者当成完全不同的东西

## 复盘问题

1. 今天最大的收获是什么？
2. 路由分发器相比「在中间件里手动 if/else 判断路径」好在哪？
3. 路径参数和查询参数分别适合什么场景？举一个播客应用里的例子。
4. 当前路由分发器还有哪些边界情况没有处理？（例如重复注册同一路由、路径冲突等）

## 明日预习

第 5 天将进入 Express 框架入门前的最后过渡：把今天的路由分发器与中间件执行器组合成一个更接近 Express 的 `App` 对象，理解 `app.use` 和 `app.get` 的注册顺序与执行顺序。

预习要点：
- Express 的 `app.use(middleware)` 与 `app.get(path, handler)` 注册后，请求到达时谁先执行？
- 中间件和路由的 `next()` 行为是否一致？
- 错误处理中间件为什么有 4 个参数 `(err, req, res, next)`？

## 卡点记录区

> 记录今天没解决、需要后续回顾的问题。

- 问题 1：_________________________
- 问题 2：_________________________
