# `/today` Skill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `/daily` skill 基础上增加一个快捷命令 `/today [hours]`，自动找到最新计划目录并生成下一天。

**Architecture:** 纯 Prompt Skill。`/today` skill 只负责扫描 `daily/`、解析最新 `week/day`、计算 `nextDay`，然后通过 `Skill` 工具调用 `/daily` 完成文件生成与 Git 提交。

**Tech Stack:** Claude Code custom skills, shell, Git

---

## File Structure

- `~/.claude/skills/today/SKILL.md` — 全局生效的 `/today` skill prompt。
- `.claude/skills/today.md` — 仓库内的备份/源码副本。
- `README.md` — 更新使用说明。
- `daily/` — 运行时由 `/daily` skill 生成新的 `day-XX/` 目录。

---

### Task 1: Create the `/today` skill file

**Files:**
- Create: `~/.claude/skills/today/SKILL.md`
- Create: `.claude/skills/today.md`
- Modify: none

- [ ] **Step 1: Create the skill directories**

Run:

```bash
mkdir -p ~/.claude/skills/today
mkdir -p .claude/skills
```

- [ ] **Step 2: Write the `/today` skill prompt**

Create both `~/.claude/skills/today/SKILL.md` and `.claude/skills/today.md` with the following content:

````markdown
---
name: today
description: Use when working in a repository with daily/ study plans and you want to generate the next day's plan without specifying week and day
---

# `/today` Skill

当用户调用 `/today [hours]` 时，按本 prompt 执行。

## 1. 解析 `hours`

- 如果用户没有提供，`hours = 2`。
- 如果用户提供了但 <=0，`hours = 2`。

## 2. 查找最新计划目录

使用 `Bash` 工具运行：

```bash
find daily -type d -name 'day-*' | sort -V | tail -1
```

预期输出形如 `daily/week-01/day-02`。

- 如果命令返回空（没有任何 `day-*` 目录），向用户报错并停止：
  > 未找到任何已有计划，请先使用 `/daily 1 1` 生成第一天计划。

## 3. 解析 `week` 和 `day`

从最新目录路径中提取：

- `week`：路径中 `week-XX` 的整数部分。
- `day`：路径中 `day-YY` 的整数部分。

例如 `daily/week-01/day-02` -> `week=1`, `day=2`。

## 4. 计算下一天

- `nextDay = day + 1`
- `week` 保持不变。

## 5. 调用 `/daily` skill

使用 `Skill` 工具调用 `daily`，参数为 `[week, nextDay, hours]`。

示例：
- `/today` 且最新为 `day-02` -> `Skill daily 1 3 2`
- `/today 3` 且最新为 `day-02` -> `Skill daily 1 3 3`

## 6. 输出摘要

向用户说明：

```text
已为你调用 /daily {week} {nextDay} {hours}
```

然后 `/daily` skill 会输出生成的文件列表。
````

- [ ] **Step 3: Commit the repo backup**

Run:

```bash
git add .claude/skills/today.md
git commit -m "feat: add /today skill backup"
git push
```

---

### Task 2: Validate the `/today` skill

**Files:**
- Create: `daily/week-01/day-03/*` (generated by `/daily`)
- Modify: none

- [ ] **Step 4: Invoke `/today`**

Run:

```text
/today
```

If `/today` is not recognized, use the `Skill` tool with name `today` and no arguments.

- [ ] **Step 5: Verify generated files**

Check that `daily/week-01/day-03/` exists and contains:

```text
daily/week-01/day-03/README.md
daily/week-01/day-03/tasks.md
daily/week-01/day-03/knowledge.md
daily/week-01/day-03/demo.md
daily/week-01/day-03/resources.md
daily/week-01/day-03/review.md
daily/week-01/day-03/demo/
```

Open `daily/week-01/day-03/README.md` and confirm:
- `week` is `1`.
- `day` is `3`.
- `hours` is `2`.
- `theme` matches week 1 of `full-stack-plan.md`.

- [ ] **Step 6: Verify Git operations**

Run:

```bash
git log --oneline -1
git status
```

Expected:
- Latest commit message is `docs: add daily plan for week 1 day 3`.
- Working tree is clean (or only pre-existing `README.md` modification remains).

---

### Task 3: Update README usage note

**Files:**
- Modify: `README.md`

- [ ] **Step 7: Append `/today` usage to README**

Append the following to `README.md` after the existing `/daily` section:

```markdown
### 快捷命令

- `/today` — 基于最新计划生成下一天（默认 2h）
- `/today 3` — 基于最新计划生成下一天，指定 3h
```

- [ ] **Step 8: Commit README update**

Run:

```bash
git add README.md
git commit -m "docs: add /today usage note" -- README.md
git push
```

---

## Self-Review

**1. Spec coverage:**
- `/today [hours]` command format -> Task 1 Step 2
- Scan `daily/` for latest plan -> Task 1 Step 2
- `nextDay = day + 1`, week unchanged -> Task 1 Step 2
- Default hours = 2 -> Task 1 Step 2
- Empty `daily/` error message -> Task 1 Step 2
- Call `/daily` skill -> Task 1 Step 2
- Validation by generating day 3 -> Task 2

**2. Placeholder scan:**
- No TBD / TODO
- All commands and file content are concrete
- No "similar to Task N" references

**3. Type consistency:**
- `week`, `day`, `hours` semantics consistent with `/daily` skill
- Directory naming `week-XX/day-YY` consistent

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-06-23-today-skill.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
